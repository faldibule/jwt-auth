<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="border: none;" >
  <div class="container" >
    <img src="/img/logo.png" style="width: 50px; margin-right: 10px;" alt="logo">
    <a class="navbar-brand" href="#">Blogopedia</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/">About</a>
        </li>
      </ul>
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <% if (cekNav) { %>
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Hallo, <%=user.nama.split(" ")[0]; %> <img width="30" style="height: 30px; object-fit:cover; object-position: center; " class="rounded-circle" src="<%= user.image  %>" />
            </a>
          <% }else{ %>
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Menu
            </a>
          <% } %> 
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><a class="nav-link text-dark" href="/post"><i class="bi bi-signpost-2-fill"></i> Post</a></li>
            <li><hr class="dropdown-divider" /></li>
            <% if (cekNav) { %>
            <li><a href="/profile" class="nav-link text-dark"><i class="bi bi-person"></i> Profile</a></li>
            <li><hr class="dropdown-divider" /></li>
            <li><a href="/auth/logout" class="nav-link text-dark logout" onclick="window.location.reload();"><i class="bi bi-box-arrow-in-right"></i> Log Out</a></li>
            <% }else{ %>
            <li><a href="/auth/login" class="nav-link text-dark"><i class="bi bi-box-arrow-in-left"></i> Log In</a></li>
            <% } %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
